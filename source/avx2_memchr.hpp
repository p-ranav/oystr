#pragma once

namespace search
{
const char* find_avx2_more(const char* b,
                           const char* e,
                           char c,
                           bool ignore_case);
}
